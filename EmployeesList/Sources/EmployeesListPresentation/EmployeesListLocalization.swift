public struct EmployeesListLocalization {
    let mainTitle: String
    let genericError: String
    let okayButtonTitle: String
    
    public init(
        mainTitle: String,
        genericError: String,
        okayButtonTitle: String
    ) {
        self.mainTitle = mainTitle
        self.genericError = genericError
        self.okayButtonTitle = okayButtonTitle
    }
}
